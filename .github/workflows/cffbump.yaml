name: "update cff"

# On releases
on:
  push:
    branches:
      - 'main'

- uses: btraven00/cffbump@v1
  with:
    git-min-commits: '1'
    include-sources: 'git,crossref'
    doi: '10.1371/journal.pone.0021373'
